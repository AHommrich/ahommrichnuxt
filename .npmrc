# Nuxt 4 zieht @nuxt/eslint-config mit einem eslint-^10-Peer, das Projekt bleibt
# vorerst auf eslint 9. legacy-peer-deps macht diesen Konflikt für npm install
# und npm ci (CI) unkritisch, ohne die eslint-Version zu erzwingen.
legacy-peer-deps=true
