ahommrich.de {
    reverse_proxy nuxt-app:3000
    tls andre-hommrich@web.de  # E-Mail-Adresse für Let's Encrypt

    # Access logs are intentionally disabled — no persistent IP logging
    log {
        output discard
    }
}
